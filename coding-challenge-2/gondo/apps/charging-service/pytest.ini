[pytest]
asyncio_mode = auto
# One shared loop for all tests so the global async SQLAlchemy engine is not rebound across loops.
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = function
