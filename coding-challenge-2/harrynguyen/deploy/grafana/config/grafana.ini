# Minimal overrides for Docker (corporate TLS / air-gapped friendly).
[paths]
provisioning = /etc/grafana/provisioning

[plugins]
# Empty: do not fetch optional bundled plugins from grafana.com on startup.
preinstall =
