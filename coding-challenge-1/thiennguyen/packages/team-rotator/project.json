{
  "name": "team-rotator",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/team-rotator/src",
  "projectType": "library",
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf {projectRoot}/lib"
      }
    },
    "build": {
      "dependsOn": ["clean"],
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/lib/packages/team-rotator"],
      "options": {
        "command": "tsc -p packages/team-rotator/tsconfig.lib.json"
      },
      "parallelism": false
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/team-rotator/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  },
  "tags": []
}
